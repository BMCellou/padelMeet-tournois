import { describe, expect, it } from "vitest";

describe("moteur de tournoi (lot 0 — smoke test)", () => {
  it("vitest est câblé et prêt pour le lot 1", () => {
    expect(true).toBe(true);
  });
});
